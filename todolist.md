# TODO

- [x] 实现论文统一识别与跨用户上传去重
  - 线上下载的论文使用规范化 DOI 或 arXiv ID 识别同一论文。
  - 用户上传的文件计算 SHA-256，识别字节完全相同的 PDF。
  - 上传文件解析后提取 DOI/arXiv ID，并关联到已有论文记录。
  - 区分论文身份与 PDF 文件版本，避免将 arXiv、正式发表版和用户上传版误当成不同论文。
  - 跨用户复用文件和解析结果时保留独立访问权限，不向用户泄露其他用户的上传记录。

- [x] 完善论文删除的跨存储一致性与垃圾清理
  - 普通删除仅解除当前用户的 `UserPaper`/`UserUpload` 关联，并清理该用户在 Milvus 中的论文向量。
  - 仍有其他用户引用时保留共享论文元数据、chunks、Elasticsearch BM25 索引和 PDF 文件。
  - 最后一个引用删除后，清理 Milvus 全部相关向量、Elasticsearch 文档、PostgreSQL 全文数据及无引用文件。
  - DOI/arXiv 公共论文允许保留轻量元数据，私有上传论文在无引用后可彻底删除。
  - 使用逻辑删除和可重试的 `cleanup_job`/outbox，记录各存储清理状态，避免产生半删除或孤儿索引。
  - 删除或失效相关 RAG 缓存，并补充权限隔离、并发删除和失败重试测试。

- [ ] 将 MinerU 的结构化 JSON 作为 PDF 标准解析产物
  - 从 MinerU ZIP 中读取并保存结构化 JSON，同时保留由 JSON 派生的 Markdown 以兼容现有流程。
  - 增加项目内部稳定的 `DocumentBlock` 模型，避免下游直接依赖 MinerU 的版本化字段。
  - 保留 block 类型、页码、页面内顺序、标题层级、章节路径、边界框及表格/公式/图片说明等信息。
  - 使用结构化 blocks 生成 chunks，向量化时仅嵌入清洗后的正文，结构信息作为 metadata 保存。
  - 为本地 PyMuPDF 回退解析提供相同的标准输出接口，并记录 `parser_version`/`schema_version`。

- [ ] 修复论文全文前端显示乱序并分离展示数据与检索 chunks
  - 修复当前每个章节的 chunk `ordinal` 都从 0 开始、查询仅按 `ordinal` 排序导致的非确定性顺序。
  - 增加 `section_ordinal`、`chunk_ordinal` 和全局 `document_ordinal`，查询使用稳定的全局顺序。
  - 前端全文使用按页码和 block 顺序排列的无重叠 blocks 或派生 Markdown，不再从带 overlap 的 RAG chunks 拼接。
  - 保留 RAG chunks 仅用于向量/BM25 检索，并通过 `source_block_ids` 关联原始展示内容。
  - 迁移或重新解析已有论文，并覆盖多栏 PDF、重复章节名、跨页段落、表格和公式的顺序测试。

- [x] 实现幻觉检测 Verifier 子 Agent
  - 在主 Agent 完成初始 RAG 检索并生成回答草稿后启动验证，不直接验证尚未形成的 RAG 查询结果。
  - 将回答拆分为可独立验证的原子事实，优先检查数字、比较、因果关系、方法归属和论文结论。
  - 针对每条高风险事实执行反向 RAG，记录 `paper_id`、`chunk_id`、原文证据、相似度、页码或章节，并保持用户论文权限隔离。
  - 将事实判定为 `SUPPORTED`、`CONTRADICTED` 或 `NOT_ENOUGH_EVIDENCE`；未检索到证据不得直接判为错误。
  - 根据事实重要性和判定结果聚合风险，同时保留最高单条风险，避免严重错误被平均分稀释。
  - 根据证据修正回答：保留并引用已支持事实，删除或改写冲突事实，对证据不足的事实删除或弱化表述。
  - 修正阶段不得引入新的未验证事实，输出结构化验证报告和修正后的回答。
  - 新增只读 `verifier` 子 Agent 类型，仅允许使用 RAG 和只读工具；MVP 每次最多验证 10～15 条高风险事实，限制检索并发和总耗时。
  - 增加原子事实拆解、权限隔离、三类判定、风险聚合、回答修正和降级行为测试。
